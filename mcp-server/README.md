# Risebyeden MCP Server

Minimal MCP server with Sentry instrumentation.

## Setup

1. Create a Sentry project and copy the DSN.
2. In this folder, add a `.env` file:

```
SENTRY_DSN=your_dsn_here
```

## Run

```
npm install
npm run start
```
